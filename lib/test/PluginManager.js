
suite("PluginManager - Loads plugins that provide additional functionality to sandbox", function(){
    test("Loads local plugins");
    test("Loads external plugins");
    test("Calls hooks successfully");
});
